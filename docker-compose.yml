version: '3'

services:
  python-app:
    build: .
    command: gunicorn -w 4 -b 0.0.0.0:5000 app:app
    ports:
      - "8089:5000"

# 注释掉 Nginx 相关的部分
#  nginx:
#    image: nginx:latest
#    ports:
#      - "8089:80"
#    volumes:
#      - ./nginx.conf:/etc/nginx/nginx.conf:ro
#    depends_on:
#      - python-app
